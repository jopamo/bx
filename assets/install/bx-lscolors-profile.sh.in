# Generated by bx install.
# Sets LS_COLORS from the installed bx dircolors database.

if [ -n "${LS_COLORS-}" ]; then
    return 0 2>/dev/null || true
fi

if command -v dircolors >/dev/null 2>&1 && [ -r "@LSCOLORS_FILE@" ]; then
    eval "$(dircolors -b \"@LSCOLORS_FILE@\")"
fi
