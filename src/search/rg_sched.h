#ifndef BX_SEARCH_RG_SCHED_H
#define BX_SEARCH_RG_SCHED_H

/*
 * Placeholder for the worker-local scheduler split. Phase 1 only isolates the
 * existing rg parallel path into dedicated modules without changing behavior.
 */

#endif
